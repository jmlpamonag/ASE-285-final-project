<!DOCTYPE html>
<html>
    <body>

                    <p>###POSTS</p>
                    <% if (posts.length > 0) { %>
                    <% posts.forEach(post => {%>
                       <p>**Title:** <%= post.title %></p>
                       <p>**Date:** <%= post.date %></p>
                       <p>**Description:** <%= post.description %></p>
                       <br>
                    <%  });%>
                <% }%>

    </body>
</html>